<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Yadro - Premium Headphones</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    /* Enhanced Animations */
    @keyframes float {

      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-20px);
      }
    }

    @keyframes shine {
      0% {
        background-position: -200% center;
      }

      100% {
        background-position: 200% center;
      }
    }

    @keyframes scaleIn {
      from {
        transform: scale(0.9);
        opacity: 0;
      }

      to {
        transform: scale(1);
        opacity: 1;
      }
    }

    @keyframes slideUp {
      from {
        transform: translateY(20px);
        opacity: 0;
      }

      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    /* Enhanced Hover Effects */
    .product-card {
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .product-card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }

    .category-card {
      transition: all 0.3s ease;
    }

    .category-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
    }

    /* Shimmer Effect */
    .shimmer {
      background: linear-gradient(90deg,
          rgba(255, 255, 255, 0) 0%,
          rgba(255, 255, 255, 0.2) 50%,
          rgba(255, 255, 255, 0) 100%);
      background-size: 200% 100%;
      animation: shine 1.5s infinite;
    }

    /* Enhanced Scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: #f1f1f1;
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb {
      background: #DB2777;
      border-radius: 4px;
    }

    /* Smooth Scroll */
    html {
      scroll-behavior: smooth;
    }

    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      transition: all 0.6s ease-out;
    }

    .fade-in.visible {
      opacity: 1;
      transform: translateY(0);
    }


    /* Enhanced Responsive Animations */
    @keyframes float {

      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-20px);
      }
    }

    @keyframes shine {
      0% {
        background-position: -200% center;
      }

      100% {
        background-position: 200% center;
      }
    }

    /* Mobile-First Approach */
    .nav-menu {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.9);
      z-index: 999;
    }

    .nav-menu.active {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    /* Base Animations */
    @keyframes float {

      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-20px);
      }
    }

    @keyframes shine {
      0% {
        background-position: -200% center;
      }

      100% {
        background-position: 200% center;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive Classes */
    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      transition: all 0.6s ease-out;
    }

    .fade-in.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* Enhanced Mobile Menu */
    .mobile-menu {
      transform: translateX(-100%);
      transition: transform 0.3s ease-in-out;
    }

    .mobile-menu.active {
      transform: translateX(0);
    }

    /* Product Card Enhancements */
    .product-card {
      transition: all 0.3s ease;
      height: 100%;
    }

    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    }

    /* Responsive Images */
    .product-image {
      aspect-ratio: 1;
      object-fit: contain;
    }

    /* Custom Scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: #f1f1f1;
    }

    ::-webkit-scrollbar-thumb {
      background: #DB2777;
      border-radius: 4px;
    }

    /* Media Queries */
    @media (max-width: 768px) {
      .desktop-menu {
        display: none;
      }

      .hero-content {
        text-align: center;
        padding: 2rem !important;
      }

      .hero-image {
        max-width: 300px;
        margin: 0 auto;
      }
    }
  </style>
</head>

<body class="bg-gray-50">
  <!-- Header -->
  <header class="fixed w-full top-0 z-50 bg-white/90 backdrop-blur-md shadow-sm">
    <nav class="container mx-auto px-4 py-4">
      <div class="flex justify-between items-center">
        <!-- Logo -->
        <a href="/" class="text-2xl font-bold text-red-600 hover:text-red-700">
          YADRO
        </a>

        <!-- Desktop Navigation -->
        <div class=" hidden md:flex items-center space-x-6">
          <a href="#" class="nav-link hover:text-red-600 transition-colors">Home</a>
          <a href="#categories" class="nav-link hover:text-red-600 transition-colors">Categories</a>
          <a href="#latest" class="nav-link hover:text-red-600 transition-colors">Latest</a>
          <a href="#popular" class="nav-link hover:text-red-600 transition-colors">Popular</a>
        </div>

        <!-- Action Buttons -->
        <div class="flex items-center space-x-4">
          <button class="p-2 hover:bg-gray-100 rounded-full transition-all duration-300">
            <i class="fas fa-search text-gray-600"></i>
          </button>

          <button id="cartBtn" class="p-2 hover:bg-gray-100 rounded-full transition-all duration-300 relative">
            <i class="fas fa-shopping-cart text-gray-600"></i>
            <span class="absolute -top-1 -right-1 bg-red-600 text-white text-xs rounded-full w-4 h-4 flex items-center justify-center">0</span>
          </button>


          <!-- Auth Buttons (Hidden on Mobile) -->
          <div class="md:flex items-center space-x-4">
            <a href="/login" class="bg-red-600 text-white px-6 py-2 rounded-full hover:bg-red-700 transition-all duration-300">
              Login
            </a>
            <a href="/signup" class="bg-gray-800 text-white px-6 py-2 rounded-full hover:bg-gray-900 transition-all duration-300">
              Sign Up
            </a>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div class="mobile-menu fixed inset-0 bg-white z-50 hidden">
        <div class="flex justify-end p-4">
          <button onclick="toggleMobileMenu()">
            <i class="fas fa-times text-xl"></i>
          </button>
        </div>
        <div class="flex flex-col items-center space-y-6 p-8">
          <a href="#" class="text-xl">Home</a>
          <a href="#categories" class="text-xl">Categories</a>
          <a href="#latest" class="text-xl">Latest</a>
          <a href="#popular" class="text-xl">Popular</a>
          <button onclick="openModal('loginModal')" class="w-full bg-red-600 text-white px-6 py-2 rounded-full">
            Login
          </button>
          <button onclick="openModal('signupModal')" class="w-full bg-gray-800 text-white px-6 py-2 rounded-full">
            Sign Up
          </button>
        </div>
      </div>
    </nav>
  </header>

  <!-- Cart Modal -->
  <div id="cartModal" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50">
    <div class="fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 bg-white rounded-lg p-8 w-96 opacity-0 transition-all duration-300">
      <div class="text-center">
        <div class="mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
        </div>
        <h2 class="text-2xl font-bold mb-4">Authentication Required</h2>
        <p class="text-gray-600 mb-6">Please login or sign up to access your cart</p>
        <div class="flex space-x-4 justify-center">
          <a href="/login" class="bg-red-600 text-white px-6 py-2 rounded-full hover:bg-red-700 transition-all duration-300">
            Login
          </a>
          <a href="/signup" class="bg-gray-800 text-white px-6 py-2 rounded-full hover:bg-gray-900 transition-all duration-300">
            Sign Up
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <main class="container mx-auto px-4 pt-24">
    <!-- Hero Section -->
    <section class="relative min-h-[500px] md:h-[600px] rounded-3xl overflow-hidden mb-16 bg-gradient-to-r from-red-600 to-purple-600">
      <div class="absolute inset-0 bg-pattern opacity-10"></div>
      <div class="relative h-full flex flex-col md:flex-row items-center">
        <div class="w-full md:w-1/2 p-8 md:p-16 text-white text-center md:text-left">
          <h1 class="text-4xl md:text-6xl font-bold mb-6 fade-in">Experience Pure Sound</h1>
          <p class="text-lg md:text-xl mb-8 fade-in">Discover our premium collection of headphones designed for audiophiles.</p>
          <button class="bg-white text-red-600 px-8 py-3 rounded-full hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 fade-in">
            Shop Now
          </button>
        </div>
        <div class="w-full md:w-1/2 relative p-8">
          <img src="/images/headphone.png" alt="Premium Headphones" class="hero-image mx-auto" style="animation: float 6s ease-in-out infinite;">
        </div>
      </div>
    </section>

    <!-- Categories Section -->
    <section id="categories" class="mb-16">
      <h2 class="text-3xl font-bold mb-8 fade-in">Browse Categories</h2>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
        <% ['Wireless', 'Sport', 'Professional', 'Gaming'].forEach(category => { %>
        <div class="category-card bg-white rounded-xl p-6 text-center cursor-pointer fade-in hover:shadow-lg transition-all duration-300">
          <div class="mb-4">
            <img src="/images/dummy.png" alt="<%= category %>" class="w-24 h-24 mx-auto object-contain">
          </div>
          <h3 class="font-semibold"><%= category %></h3>
        </div>
        <% }) %>
      </div>
    </section>

    <!-- Latest Products Section -->
    <section id="latest" class="mb-16">
      <div class="flex justify-between items-center mb-8">
        <h2 class="text-3xl font-bold fade-in">Latest Arrivals</h2>
        <div class="flex space-x-2">
          <button class="p-2 rounded-full bg-gray-100 hover:bg-red-600 hover:text-white transition-all duration-300">
            <i class="fas fa-chevron-left"></i>
          </button>
          <button class="p-2 rounded-full bg-gray-100 hover:bg-red-600 hover:text-white transition-all duration-300">
            <i class="fas fa-chevron-right"></i>
          </button>
        </div>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
        <% latestProducts.forEach(product => { %>
        <div class="product-card bg-white rounded-xl p-6 fade-in">
          <div class="relative mb-4 overflow-hidden rounded-lg">
            <img src="<%= product.images[0] %>" alt="Product" class="w-full h-48 object-contain">
            <div class="absolute top-2 right-2">
              <button class="p-2 bg-white rounded-full hover:bg-red-600 hover:text-white transition-all duration-300">
                <i class="fas fa-heart"></i>
              </button>
            </div>
          </div>
          <h3 class="font-semibold text-lg mb-2"><%= product.name %></h3>
          <div class="flex items-center justify-between mb-4">
            <div class="flex items-center space-x-2">
              <span class="text-red-600 font-bold">₹ <%= product.salePrice %></span>
              <span class="text-gray-400 line-through">₹ <%= product.regularPrice%></span>
            </div>
            <div class="flex items-center">
              <% Array(5).fill().forEach(() => { %>
              <i class="fas fa-star text-yellow-400 text-sm"></i>
              <% }) %>
            </div>
          </div>
          <button class="w-full bg-gray-100 text-gray-700 py-2 rounded-lg hover:bg-red-600 hover:text-white transition-all duration-300">
            Add to Cart
          </button>
        </div>
        <% }) %>
      </div>
    </section>

    <!-- Popular Products -->
    <section id="popular" class="mb-16">
      <h2 class="text-3xl font-bold mb-8 fade-in">Most Popular</h2>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <% popularProducts.forEach(product => { %>
        <div class="product-card bg-white rounded-xl p-6 fade-in">
          <!-- Similar structure to Latest Arrivals products -->
          <div class="relative mb-4 overflow-hidden rounded-lg">
            <img src="<%= product.images[0] %>" alt="<%= product.name %>" class="w-full h-48 object-contain">
            <div class="absolute top-2 right-2">
              <button class="p-2 bg-white rounded-full hover:bg-red-600 hover:text-white transition-all duration-300">
                <i class="fas fa-heart"></i>
              </button>
            </div>
          </div>
          <h3 class="font-semibold text-lg mb-2"><%= product.name %></h3>
          <div class="flex items-center justify-between mb-4">
            <div class="flex items-center space-x-2">
              <span class="text-red-600 font-bold">₹<%= product.salePrice %></span>
              <span class="text-gray-400 line-through">₹<%= product.regularPrice %></span>
            </div>
            <div class="flex items-center">
              <% for(let i = 0; i < 5; i++) { %>
              <i class="fas fa-star text-yellow-400 text-sm"></i>
              <% } %>
            </div>
          </div>
          <button class="w-full bg-gray-100 text-gray-700 py-2 rounded-lg hover:bg-red-600 hover:text-white transition-all duration-300">
            Add to Cart
          </button>
        </div>
        <% }) %>
      </div>
    </section>

    <!-- Budget Friendly -->
    <section id="budget" class="mb-16">
      <h2 class="text-3xl font-bold mb-8 fade-in">Budget Friendly</h2>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <% budgetProducts.forEach(product => { %>
        <div class="product-card bg-white rounded-xl p-6 fade-in">
          <!-- Similar structure to other product cards -->
          <div class="relative mb-4 overflow-hidden rounded-lg">
            <img src="<%= product.images[0] %>" alt="<%= product.name %>" class="w-full h-48 object-contain">
            <div class="absolute top-2 right-2">
              <button class="p-2 bg-white rounded-full hover:bg-red-600 hover:text-white transition-all duration-300">
                <i class="fas fa-heart"></i>
              </button>
            </div>
            <div class="absolute top-2 left-2 bg-green-500 text-white px-2 py-1 rounded-full text-xs">
              Best Value
            </div>
          </div>
          <h3 class="font-semibold text-lg mb-2"><%= product.name %></h3>
          <div class="flex items-center justify-between mb-4">
            <div class="flex items-center space-x-2">
              <span class="text-red-600 font-bold">₹<%= product.salePrice %></span>
              <span class="text-gray-400 line-through">₹<%= product.regularPrice %></span>
            </div>
            <div class="flex items-center">
              <% for(let i = 0; i < 5; i++) { %>
              <i class="fas fa-star text-yellow-400 text-sm"></i>
              <% } %>
            </div>
          </div>
          <button class="w-full bg-gray-100 text-gray-700 py-2 rounded-lg hover:bg-red-600 hover:text-white transition-all duration-300">
            Add to Cart
          </button>
        </div>
        <% }) %>
      </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="mb-16">
      <h2 class="text-3xl font-bold mb-8 fade-in">Why Choose Yadro?</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
        <div class="service-card bg-white rounded-xl p-8 text-center fade-in hover:shadow-lg transition-all duration-300">
          <div class="mb-6 text-red-600">
            <i class="fas fa-truck text-4xl"></i>
          </div>
          <h3 class="text-xl font-semibold mb-3">Free Delivery</h3>
          <p class="text-gray-600">On orders above ₹999</p>
        </div>

        <div class="service-card bg-white rounded-xl p-8 text-center fade-in hover:shadow-lg transition-all duration-300">
          <div class="mb-6 text-red-600">
            <i class="fas fa-headset text-4xl"></i>
          </div>
          <h3 class="text-xl font-semibold mb-3">24/7 Support</h3>
          <p class="text-gray-600">Expert assistance</p>
        </div>

        <div class="service-card bg-white rounded-xl p-8 text-center fade-in hover:shadow-lg transition-all duration-300">
          <div class="mb-6 text-red-600">
            <i class="fas fa-shield-alt text-4xl"></i>
          </div>
          <h3 class="text-xl font-semibold mb-3">Secure Payment</h3>
          <p class="text-gray-600">100% secure checkout</p>
        </div>

        <div class="service-card bg-white rounded-xl p-8 text-center fade-in hover:shadow-lg transition-all duration-300">
          <div class="mb-6 text-red-600">
            <i class="fas fa-exchange-alt text-4xl"></i>
          </div>
          <h3 class="text-xl font-semibold mb-3">Easy Returns</h3>
          <p class="text-gray-600">30-day return policy</p>
        </div>
      </div>
    </section>

    <!-- Newsletter Section -->
    <section class="mb-16 bg-gradient-to-r from-red-600 to-purple-600 rounded-3xl p-8 md:p-12 text-white">
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-3xl font-bold mb-4 fade-in">Stay Updated</h2>
        <p class="mb-8 fade-in">Subscribe to our newsletter for exclusive offers</p>
        <form class="flex flex-col md:flex-row gap-4 max-w-md mx-auto fade-in">
          <input type="email" placeholder="Enter your email" class="flex-1 px-4 py-2 rounded-full focus:outline-none focus:ring-2 focus:ring-red-400 text-gray-800">
          <button class="bg-white text-red-600 px-6 py-2 rounded-full hover:bg-gray-100 transition-all duration-300">
            Subscribe
          </button>
        </form>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-12">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <div>
          <img src="/api/placeholder/120/40" alt="Yadro Logo" class="h-8 mb-6">
          <p class="text-gray-400">Experience the perfect blend of style and sound with Yadro's premium headphones.</p>
        </div>
        <div>
          <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
          <ul class="space-y-2">
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">About Us</a></li>
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">Contact</a></li>
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">Blog</a></li>
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">FAQs</a></li>
          </ul>
        </div>
        <div>
          <h3 class="text-lg font-semibold mb-4">Customer Service</h3>
          <ul class="space-y-2">
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">Shipping Policy</a></li>
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">Returns & Exchanges</a></li>
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">Terms & Conditions</a></li>
            <li><a href="#" class="text-gray-400 hover:text-red-600 transition-colors">Privacy Policy</a></li>
          </ul>
        </div>
        <div>
          <h3 class="text-lg font-semibold mb-4">Connect With Us</h3>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-400 hover:text-red-600 transition-colors"><i class="fab fa-facebook-f"></i></a>
            <a href="#" class="text-gray-400 hover:text-red-600 transition-colors"><i class="fab fa-twitter"></i></a>
            <a href="#" class="text-gray-400 hover:text-red-600 transition-colors"><i class="fab fa-instagram"></i></a>
            <a href="#" class="text-gray-400 hover:text-red-600 transition-colors"><i class="fab fa-youtube"></i></a>
          </div>
        </div>
      </div>
      <div class="border-t border-gray-800 mt-12 pt-8 text-center text-gray-400">
        <p>&copy; 2024 Yadro. All rights reserved.</p>
      </div>
    </div>
  </footer>

  <!-- JavaScript -->
  <script>
    // Intersection Observer for fade-in animations
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, observerOptions);

    // Observe all fade-in elements
    document.querySelectorAll('.fade-in').forEach(element => {
      observer.observe(element);
    });

    // Smooth scroll for navigation
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Add to Cart Animation
    document.querySelectorAll('.product-card button').forEach(button => {
      button.addEventListener('click', function(e) {
        const cart = document.querySelector('.fa-shopping-cart');
        const cartCount = cart.nextElementSibling;

        // Animate button
        this.classList.add('animate-pulse');
        setTimeout(() => this.classList.remove('animate-pulse'), 1000);

        // Update cart count
        const currentCount = parseInt(cartCount.textContent);
        cartCount.textContent = currentCount + 1;

        // Animate cart icon
        cart.parentElement.classList.add('animate-bounce');
        setTimeout(() => cart.parentElement.classList.remove('animate-bounce'), 1000);
      });
    });

    // Mobile Menu Toggle
    const mobileMenuButton = document.querySelector('.mobile-menu-button');
    const mobileMenu = document.querySelector('.mobile-menu');

    if (mobileMenuButton && mobileMenu) {
      mobileMenuButton.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
      });
    }

    document.addEventListener('DOMContentLoaded', function() {
      // Cart button click handler
      document.getElementById('cartBtn').addEventListener('click', function() {
        openModal('cartModal');
      });

      // Close modals when clicking outside
      document.querySelectorAll('#cartModal, #loginModal, #signupModal').forEach(modal => {
        modal.addEventListener('click', function(e) {
          if (e.target === this) {
            closeModal(this.id);
          }
        });
      });
    });

    function openModal(modalId) {
      const modal = document.getElementById(modalId);
      const modalContent = modal.querySelector('div');

      // First make the modal visible
      modal.classList.remove('hidden');

      // Then trigger the animation
      setTimeout(() => {
        modalContent.classList.remove('opacity-0');
        modalContent.classList.add('opacity-100');
      }, 10);
    }

    function closeModal(modalId) {
      const modal = document.getElementById(modalId);
      const modalContent = modal.querySelector('div');

      // First fade out
      modalContent.classList.remove('opacity-100');
      modalContent.classList.add('opacity-0');

      // Then hide the modal
      setTimeout(() => {
        modal.classList.add('hidden');
      }, 300);
    }

    // Mobile Menu Toggle
    function toggleMobileMenu() {
      const mobileMenu = document.querySelector('.mobile-menu');
      mobileMenu.classList.toggle('hidden');
    }

    // Close mobile menu when clicking a link
    document.querySelectorAll('.mobile-menu a').forEach(link => {
      link.addEventListener('click', () => {
        document.querySelector('.mobile-menu').classList.add('hidden');
      });
    });

    // Responsive Image Loading
    function handleResponsiveImages() {
      const images = document.querySelectorAll('img[data-src]');
      images.forEach(img => {
        if (window.innerWidth <= 768) {
          img.src = img.getAttribute('data-src-mobile');
        } else {
          img.src = img.getAttribute('data-src');
        }
      });
    }

    // Initialize responsive features
    window.addEventListener('load', handleResponsiveImages);
    window.addEventListener('resize', handleResponsiveImages);

    document.querySelectorAll('.fade-in').forEach(element => {
      observer.observe(element);
    });
  </script>
</body>

</html>