<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Yadro - HeadPhones</title>
  <link rel="stylesheet" href="/css/home-without-user.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" href="/images/logo.png" type="image/x-icon">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
  <script src="/js/product-card.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    @keyframes float {

      0%,
      100% {
        transform: translateY(0) rotate(0);
      }

      50% {
        transform: translateY(-10px) rotate(5deg);
      }
    }

    @keyframes pulse {

      0%,
      100% {
        transform: scale(1);
      }

      50% {
        transform: scale(1.05);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translate3d(0, 50px, 0);
      }

      to {
        opacity: 1;
        transform: translate3d(0, 0, 0);
      }
    }

    .category-item {
      opacity: 0;
      animation: fadeInUp 0.6s ease-out forwards;
    }

    .category-item:hover .icon-wrapper {
      animation: float 3s ease-in-out infinite;
    }

    .perspective-1000 {
      perspective: 1000px;
    }

    .category-item:hover>div {
      transform: scale(1.05) rotateY(10deg);
      box-shadow: 0 20px 30px rgba(0, 0, 0, 0.1);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in {
      animation: fadeIn 0.5s ease-out;
    }


    .like-button {
      transition: all 0.3s ease;
    }

    .like-button:hover {
      transform: scale(1.2);
    }

    .like-button svg {
      transition: fill 0.3s ease, transform 0.3s ease;
    }

    .like-button:hover svg {
      transform: scale(1.2);
    }

    @keyframes float {

      0%,
      100% {
        transform: translateY(0) rotate(0);
      }

      50% {
        transform: translateY(-10px) rotate(5deg);
      }
    }

    @keyframes pulse {

      0%,
      100% {
        transform: scale(1);
      }

      50% {
        transform: scale(1.05);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translate3d(0, 50px, 0);
      }

      to {
        opacity: 1;
        transform: translate3d(0, 0, 0);
      }
    }

    .category-item {
      opacity: 0;
      animation: fadeInUp 0.6s ease-out forwards;
    }

    .category-item:hover .icon-wrapper {
      animation: float 3s ease-in-out infinite;
    }

    .perspective-1000 {
      perspective: 1000px;
    }

    .category-item:hover>div {
      transform: scale(1.05) rotateY(10deg);
      box-shadow: 0 20px 30px rgba(0, 0, 0, 0.1);
    }


    .gradient-text {
      background: linear-gradient(to right, #dc2626, #ef4444, #f87171);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      position: relative;
      display: inline-block;
      padding: 0.25rem 0;
    }

    .gradient-line {
      height: 4px;
      width: 6rem;
      background: linear-gradient(to right, #dc2626, #ef4444);
      border-radius: 9999px;
    }

    .heading-container {
      padding: 1.25rem;
      animation: fadeIn 0.5s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .category-item {
      animation: fadeInUp 0.6s ease-out forwards;
      opacity: 0;
    }

    /* Hover Effect for Icon */
    .icon-wrapper::after {
      content: '';
      position: absolute;
      inset: -4px;
      background: linear-gradient(45deg, #EF4444, #F43F5E);
      border-radius: 50%;
      z-index: -1;
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .group:hover .icon-wrapper::after {
      opacity: 0.2;
    }

    /* Pulse Animation on Hover */
    @keyframes softPulse {
      0% {
        box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4);
      }

      70% {
        box-shadow: 0 0 0 10px rgba(239, 68, 68, 0);
      }

      100% {
        box-shadow: 0 0 0 0 rgba(239, 68, 68, 0);
      }
    }

    /* Floating Animation */
    @keyframes float {

      0%,
      100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-5px);
      }
    }

    .group:hover .icon-wrapper {
      animation: softPulse 2s infinite, float 3s ease-in-out infinite;
    }

    /* Shine Effect */
    .category-item::before {
      content: '';
      position: absolute;
      top: 0;
      left: -75%;
      z-index: 2;
      width: 50%;
      height: 100%;
      background: linear-gradient(to right,
          rgba(255, 255, 255, 0) 0%,
          rgba(255, 255, 255, 0.3) 100%);
      transform: skewX(-25deg);
      transition: all 0.75s;
    }

    .category-item:hover::before {
      animation: shine 1.5s infinite;
    }

    @keyframes shine {
      100% {
        left: 125%;
      }
    }


    @keyframes gradientFlow {
      0% {
        background-position: 0% 50%;
      }

      50% {
        background-position: 100% 50%;
      }

      100% {
        background-position: 0% 50%;
      }
    }

    .heading-container h2 {
      background-size: 200% auto;
      animation: gradientFlow 5s ease infinite;
    }

    /* Shine Effect for Line */
    @keyframes shineLine {
      0% {
        transform: translate(-50%, 0) scale(0.8);
        opacity: 0.5;
      }

      50% {
        transform: translate(-50%, 0) scale(1.2);
        opacity: 1;
      }

      100% {
        transform: translate(-50%, 0) scale(0.8);
        opacity: 0.5;
      }
    }

    .shine-line {
      animation: shineLine 3s ease-in-out infinite;
    }

    /* Floating Animation for Dots */
    @keyframes float {

      0%,
      100% {
        transform: translate(0, 0);
      }

      50% {
        transform: translate(0, -5px);
      }
    }

    .decorative-elements .rounded-full {
      animation: float 3s ease-in-out infinite;
    }

    /* Initial Load Animation */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .heading-container {
      animation: fadeInUp 0.8s ease-out forwards;
    }

    /* Additional hover effect for the whole container */
    .heading-container:hover .shine-line {
      animation: shineLine 1.5s ease-in-out infinite;
    }

    /* Custom underline effect */
    .heading-container h2::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 50%;
      width: 0;
      height: 2px;
      background: linear-gradient(to right, #DC2626, #F43F5E);
      transform: translateX(-50%);
      transition: width 0.3s ease;
      opacity: 0.5;
    }

    .heading-container:hover h2::after {
      width: 100%;
    }
  </style>
</head>

<body>
  <%- include('../layouts/header-with-user.ejs') %>
  <main class="container mx-auto px-4 py-8">

    <section class="relative min-h-[500px] md:h-[600px] rounded-3xl overflow-hidden mb-16 mt-16 bg-gradient-to-r from-red-600 to-purple-600">
      <div class="absolute inset-0 bg-pattern opacity-10"></div>
      <div class="relative h-full flex flex-col md:flex-row items-center">
        <div class="w-full md:w-1/2 p-8 md:p-16 text-white text-center md:text-left">
          <h1 class="text-4xl md:text-6xl font-bold mb-6 fade-in">Experience Pure Sound</h1>
          <p class="text-lg md:text-xl mb-8 fade-in">Discover our premium collection of headphones designed for audiophiles.</p>
          <a href="#latest-arrivals" class="bg-white text-red-600 px-8 py-3 rounded-full hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 fade-in">
            Shop Now
          </a>
        </div>
        <div class="w-full md:w-1/2 relative p-8">
          <img src="/images/headphone.png" alt="Premium Headphones" class="hero-image mx-auto" style="animation: float 6s ease-in-out infinite;">
        </div>
      </div>
    </section>


    <!-- Flash Sales -->
    <section class="mb-12" id="latest-arrivals">
      <div class="flex justify-between items-center mb-4">
        <div class="heading-container">
          <h2 class="text-4xl font-bold gradient-text mb-2">
            Latest Arrivals
          </h2>
          <p class="text-lg text-gray-600 font-medium">
            Discover our newest collection of trending items
          </p>
          <div class="gradient-line mt-2"></div>
        </div>

      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Repeat this product card structure for each item -->


        <% latestProducts.forEach(product => { %>
        <!-- Product Card -->
        <div class="product-card bg-white rounded-lg shadow-lg overflow-hidden transform hover:scale-105 transition-all duration-300 animate-fade-in">
          <!-- Image Slider -->
          <div class="relative overflow-hidden group h-64">
            <div class="image-slider flex transition-transform duration-500 ease-in-out h-full">
              <% product.images.forEach(image => { %>
              <img src="<%= image %>" alt="Product" class="w-full h-full object-cover flex-shrink-0">
              <% }) %>
            </div>
            <!-- Slider Navigation -->
            <div class="absolute inset-0 flex items-center justify-between p-4 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
              <button class="slider-arrow bg-white bg-opacity-75 hover:bg-opacity-100 rounded-full p-2 focus:outline-none transform -translate-x-10 group-hover:translate-x-0 transition-all duration-300" data-direction="prev">
                <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
              </button>
              <button class="slider-arrow bg-white bg-opacity-75 hover:bg-opacity-100 rounded-full p-2 focus:outline-none transform translate-x-10 group-hover:translate-x-0 transition-all duration-300" data-direction="next">
                <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>
            </div>
          </div>
          <a href="/product-detail/<%= product._id %>">
            <!-- Product Info -->
            <div class="p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= product.name %></h3>
              <div class="flex items-center justify-between mb-4">
                <div class="flex items-center">
                  <span class="text-2xl font-bold text-red-600">₹<%= Math.round(product.salePrice) %></span>
                  <span class="text-sm text-gray-500 line-through ml-2">₹<%= product.regularPrice %></span>
                </div>
                <% if (product.quantity > 0) { %>
                <span class="bg-red-100 text-red-800 text-xs font-semibold px-2.5 py-0.5 rounded"><%= product.productOffer %> % OFF</span>
                <% } else { %>
                <span class="bg-gray-100 text-gray-800 text-xs font-semibold px-2.5 py-0.5 rounded out-of-stock-indicator">Out of Stock</span>
                <% } %>
              </div>
              <!-- Rating Stars -->
              <div class="flex items-center mb-4">
                <div class="flex text-yellow-400">
                  <% for (let i = 1; i <= 5; i++) { %>
                  <svg class="w-5 h-5 fill-current" viewBox="0 0 24 24">
                    <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z" />
                  </svg>
                  <% } %>
                </div>
                <span class="text-gray-600 ml-2">(4.5)</span>
              </div>
          </a>
          <!-- Quantity Selector -->
          <div class="flex items-center justify-between mb-4">
            <span class="text-gray-700 font-medium">Quantity:</span>
            <div class="flex items-center border border-gray-300 rounded-full">
              <button class="quantity-btn minus w-8 h-8 flex items-center justify-center text-gray-600 hover:bg-gray-100 rounded-full focus:outline-none">-</button>
              <input type="number" id="quantity-<%= product._id %>" class="quantity-input w-12 text-center border-none focus:outline-none" value="1" min="1" max="10">

              <button class="quantity-btn plus w-8 h-8 flex items-center justify-center text-gray-600 hover:bg-gray-100 rounded-full focus:outline-none">+</button>
            </div>
          </div>

          <!-- Add to Cart Button -->
          <button data-product-id="<%= product._id %>" class="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-4 rounded-full transition duration-300 ease-in-out transform hover:scale-105 add-to-cart-btn <%= product.quantity === 0 ? 'opacity-50 cursor-not-allowed' : '' %>" <%= product.quantity === 0 ? 'disabled' : '' %>>
            <span class="add-to-cart-text flex items-center justify-center">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
              <%= product.quantity > 0 ? 'Add to Cart' : 'Out of Stock' %>
            </span>
          </button>
        </div>


        <!-- Like Button -->
        <button class="like-button absolute top-4 right-4 bg-white rounded-full p-2 shadow-md hover:bg-red-50 transition-colors duration-300" data-liked="false" data-product-id="<%= product._id %>">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
          </svg>
        </button>
      </div>

      <% }) %>



      <!-- Repeat for other products -->



      </div>
    </section>

    <!-- Categories Section -->
    <!-- Categories Section -->
    <section class="my-24 px-4 sm:px-6 lg:px-8 overflow-hidden">
      <div class="heading-container text-center relative py-8">
        <!-- Main Heading -->
        <h2 class="text-4xl md:text-5xl font-bold mb-2 bg-gradient-to-r from-red-600 to-rose-500 bg-clip-text text-transparent relative inline-block">
          Browse By Category
        </h2>

        <!-- Decorative Elements -->
        <div class="decorative-elements">
          <!-- Bottom Line with Animation -->
          <div class="relative h-1 mt-6 mb-2 mx-auto">
            <div class="absolute left-1/2 transform -translate-x-1/2 w-24 h-1 bg-gradient-to-r from-red-500 to-rose-400 rounded-full shine-line"></div>
            <!-- Side Lines -->
            <div class="absolute left-1/2 transform -translate-x-1/2 flex items-center justify-center w-48 opacity-60">
              <div class="w-8 h-px bg-red-300 transform -translate-x-8"></div>
              <div class="w-8 h-px bg-red-300 transform translate-x-8"></div>
            </div>
          </div>

          <!-- Decorative Dots -->
          <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-full pointer-events-none">
            <div class="absolute -left-4 -top-4 w-2 h-2 rounded-full bg-red-200"></div>
            <div class="absolute -right-4 -bottom-4 w-2 h-2 rounded-full bg-rose-200"></div>
          </div>
        </div>

        <!-- Optional Subtitle -->
        <p class="text-gray-600 mt-4 text-lg font-medium max-w-xl mx-auto">
          Discover our carefully curated collection of products
        </p>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-8 sm:gap-10 md:gap-12 perspective-1000">
        <% categories.forEach((category, index) => { %>
        <% if (category.isListed) { %>
        <!-- Enhanced Category Card with Red Theme -->
        <a href="/products/category/<%= category._id %>" class="category-item group" style="animation-delay: <%= index * 100 %>ms;">
          <div class="flex flex-col items-center justify-center p-8 border rounded-2xl bg-white backdrop-blur-sm transition-all duration-500 hover:scale-105 hover:shadow-2xl overflow-hidden relative h-full min-h-[300px]">
            <!-- Animated Background Elements -->
            <div class="absolute inset-0 bg-gradient-to-tr from-red-500/10 to-rose-500/10 opacity-0 group-hover:opacity-100 transition-all duration-500"></div>
            <div class="absolute -inset-[100%] bg-gradient-to-tr from-red-500/20 to-rose-500/20 rotate-45 group-hover:translate-x-full transition-transform duration-1000 ease-in-out"></div>

            <!-- Icon Wrapper with Red Gradient -->
            <div class="icon-wrapper relative mb-6 p-8 rounded-full bg-gradient-to-br from-red-50 via-red-100 to-rose-50 group-hover:from-red-100 group-hover:via-red-200 group-hover:to-rose-100 transition-all duration-500 shadow-lg group-hover:shadow-red-200/50 z-10">
              <div class="absolute inset-0 rounded-full bg-white/50 backdrop-blur-sm"></div>
              <img src="/images/dummy.png" alt="<%= category.name %>" class="w-24 h-24 object-contain transition-all duration-500 group-hover:rotate-12 group-hover:scale-110 filter group-hover:brightness-110 relative z-10">
              <!-- Decorative Ring -->
              <div class="absolute inset-0 border-4 border-red-200/30 rounded-full scale-[1.15] group-hover:scale-[1.3] group-hover:border-red-300/40 transition-all duration-500 -z-1"></div>
            </div>

            <!-- Category Name with Red Hover -->
            <h3 class="text-2xl font-semibold text-gray-800 group-hover:text-red-600 transition-all duration-300 relative z-10 text-center mb-2">
              <%= category.name %>
            </h3>

            <!-- Optional Subtle Description -->
            <p class="text-sm text-gray-500 group-hover:text-red-500 transition-all duration-300 text-center max-w-[200px] opacity-80 group-hover:opacity-100">
              Explore our collection of <%= category.name.toLowerCase() %>
            </p>

            <!-- Bottom Indicator -->
            <div class="absolute bottom-0 left-0 w-full h-1 bg-gradient-to-r from-red-500 to-rose-500 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-500 origin-left"></div>
          </div>
        </a>
        <% } %>
        <% }) %>
      </div>
    </section>


    <!-- Best Selling Products -->
    <section class="mt-12 mb-2  top-2">
      <div class="flex justify-between items-center my-4 ">
        <div class="heading-container">
          <h2 class="text-4xl font-bold gradient-text mb-2">
            Most Popular
          </h2>
          <p class="text-lg text-gray-600 font-medium">
            Discover our Most Popular items
          </p>
          <div class="gradient-line mt-2"></div>
        </div>
        <a href="/products" class="relative inline-flex items-center px-6 py-3 overflow-hidden font-bold rounded-lg group gap-2">
          <span class="relative flex items-center gap-2 z-10 text-white transition duration-300">
            View All Products
            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 transition-transform duration-300 transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </span>
          <span class="absolute bottom-0 left-0 w-full h-full transform bg-gradient-to-r from-red-600 via-red-500 to-red-400 transition-transform duration-300 ease-out -translate-x-full group-hover:translate-x-0"></span>
          <span class="absolute bottom-0 left-0 w-full h-full bg-gradient-to-r from-red-500 via-red-400 to-red-300"></span>
        </a>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Repeat product card structure from Flash Sales section -->
        <% popularProducts.forEach(product => { %>




        <!-- Product Card -->
        <div class="product-card bg-white rounded-lg shadow-lg overflow-hidden transform hover:scale-105 transition-all duration-300 animate-fade-in">
          <!-- Image Slider -->
          <div class="relative overflow-hidden group h-64">
            <div class="image-slider flex transition-transform duration-500 ease-in-out h-full">
              <% product.images.forEach(image => { %>
              <img src="<%= image %>" alt="Product" class="w-full h-full object-cover flex-shrink-0">
              <% }) %>
            </div>
            <!-- Slider Navigation -->
            <div class="absolute inset-0 flex items-center justify-between p-4 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
              <button class="slider-arrow bg-white bg-opacity-75 hover:bg-opacity-100 rounded-full p-2 focus:outline-none transform -translate-x-10 group-hover:translate-x-0 transition-all duration-300" data-direction="prev">
                <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
              </button>
              <button class="slider-arrow bg-white bg-opacity-75 hover:bg-opacity-100 rounded-full p-2 focus:outline-none transform translate-x-10 group-hover:translate-x-0 transition-all duration-300" data-direction="next">
                <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>
            </div>
          </div>
          <a href="/product-detail/<%= product._id %>">
            <!-- Product Info -->
            <div class="p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= product.name %></h3>
              <div class="flex items-center justify-between mb-4">
                <div class="flex items-center">
                  <span class="text-2xl font-bold text-red-600">₹<%= Math.round(product.salePrice) %></span>
                  <span class="text-sm text-gray-500 line-through ml-2">₹<%= product.regularPrice %></span>
                </div>
                <% if (product.quantity > 0) { %>
                <span class="bg-red-100 text-red-800 text-xs font-semibold px-2.5 py-0.5 rounded"><%= product.productOffer %> % OFF</span>
                <% } else { %>
                <span class="bg-gray-100 text-gray-800 text-xs font-semibold px-2.5 py-0.5 rounded out-of-stock-indicator">Out of Stock</span>
                <% } %>
              </div>

              <!-- Rating Stars -->
              <div class="flex items-center mb-4">
                <div class="flex text-yellow-400">
                  <% for (let i = 1; i <= 5; i++) { %>
                  <svg class="w-5 h-5 fill-current" viewBox="0 0 24 24">
                    <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z" />
                  </svg>
                  <% } %>
                </div>
                <span class="text-gray-600 ml-2">(4.5)</span>
              </div>
          </a>
          <!-- Quantity Selector -->
          <div class="flex items-center justify-between mb-4">
            <span class="text-gray-700 font-medium">Quantity:</span>
            <div class="flex items-center border border-gray-300 rounded-full">
              <button class="quantity-btn minus w-8 h-8 flex items-center justify-center text-gray-600 hover:bg-gray-100 rounded-full focus:outline-none">-</button>
              <input type="number" id="quantity-<%= product._id %>" class="quantity-input w-12 text-center border-none focus:outline-none" value="1" min="1" max="10">

              <button class="quantity-btn plus w-8 h-8 flex items-center justify-center text-gray-600 hover:bg-gray-100 rounded-full focus:outline-none">+</button>
            </div>
          </div>

          <!-- Add to Cart Button -->
          <button data-product-id="<%= product._id %>" class="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-4 rounded-full transition duration-300 ease-in-out transform hover:scale-105 add-to-cart-btn <%= product.quantity === 0 ? 'opacity-50 cursor-not-allowed' : '' %>" <%= product.quantity === 0 ? 'disabled' : '' %>>
            <span class="add-to-cart-text flex items-center justify-center">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
              <%= product.quantity > 0 ? 'Add to Cart' : 'Out of Stock' %>
            </span>
          </button>
        </div>


        <!-- Like Button -->
        <button class="like-button absolute top-4 right-4 bg-white rounded-full p-2 shadow-md hover:bg-red-50 transition-colors duration-300" data-liked="false" data-product-id="<%= product._id %>">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
          </svg>
        </button>
      </div>

      <% }) %>
      </div>
    </section>

    <!-- Enhance Your Music Experience -->
    <section class="mb-12 bg-black text-white p-16 rounded-lg relative overflow-hidden animate-on-scroll">
      <img src="/images/headphone.png" alt="Speaker" class="absolute right-20 p-3 top-0 h-full object-cover">
      <div class="relative z-10 max-w-md">
        <h2 class="text-3xl font-bold mb-4">Enhance Your Music Experience</h2>
        <button class="bg-green-500 text-white px-6 py-2 rounded-full hover:bg-green-600">Buy Now</button>
      </div>
    </section>

    <!-- Explore Our Products -->
    <section class="mb-12">
      <div class="heading-container">
        <h2 class="text-4xl font-bold gradient-text mb-2">
          Budget Friendly
        </h2>
        <p class="text-lg text-gray-600 font-medium">
          Discover our Budget Friendly items
        </p>
        <div class="gradient-line mt-2"></div>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Repeat product card structure -->
        <% budgetProducts.forEach(product => { %>

        <!-- Product Card -->
        <div class="product-card bg-white rounded-lg shadow-lg overflow-hidden transform hover:scale-105 transition-all duration-300 animate-fade-in">
          <!-- Image Slider -->
          <div class="relative overflow-hidden group h-64">
            <div class="image-slider flex transition-transform duration-500 ease-in-out h-full">
              <% product.images.forEach(image => { %>
              <img src="<%= image %>" alt="Product" class="w-full h-full object-cover flex-shrink-0">
              <% }) %>
            </div>
            <!-- Slider Navigation -->
            <div class="absolute inset-0 flex items-center justify-between p-4 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
              <button class="slider-arrow bg-white bg-opacity-75 hover:bg-opacity-100 rounded-full p-2 focus:outline-none transform -translate-x-10 group-hover:translate-x-0 transition-all duration-300" data-direction="prev">
                <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
              </button>
              <button class="slider-arrow bg-white bg-opacity-75 hover:bg-opacity-100 rounded-full p-2 focus:outline-none transform translate-x-10 group-hover:translate-x-0 transition-all duration-300" data-direction="next">
                <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </button>
            </div>
          </div>
          <a href="/product-detail/<%= product._id %>">
            <!-- Product Info -->
            <div class="p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= product.name %></h3>
              <div class="flex items-center justify-between mb-4">
                <div class="flex items-center">
                  <span class="text-2xl font-bold text-red-600">₹<%= Math.round(product.salePrice) %></span>
                  <span class="text-sm text-gray-500 line-through ml-2">₹<%= product.regularPrice %></span>
                </div>
                <% if (product.quantity > 0) { %>
                <span class="bg-red-100 text-red-800 text-xs font-semibold px-2.5 py-0.5 rounded"><%= product.productOffer %> % OFF</span>
                <% } else { %>
                <span class="bg-gray-100 text-gray-800 text-xs font-semibold px-2.5 py-0.5 rounded out-of-stock-indicator">Out of Stock</span>
                <% } %>
              </div>

              <!-- Rating Stars -->
              <div class="flex items-center mb-4">
                <div class="flex text-yellow-400">
                  <% for (let i = 1; i <= 5; i++) { %>
                  <svg class="w-5 h-5 fill-current" viewBox="0 0 24 24">
                    <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z" />
                  </svg>
                  <% } %>
                </div>
                <span class="text-gray-600 ml-2">(4.5)</span>
              </div>
          </a>
          <!-- Quantity Selector -->
          <div class="flex items-center justify-between mb-4">
            <span class="text-gray-700 font-medium">Quantity:</span>
            <div class="flex items-center border border-gray-300 rounded-full">
              <button class="quantity-btn minus w-8 h-8 flex items-center justify-center text-gray-600 hover:bg-gray-100 rounded-full focus:outline-none">-</button>
              <input type="number" id="quantity-<%= product._id %>" class="quantity-input w-12 text-center border-none focus:outline-none" value="1" min="1" max="10">

              <button class="quantity-btn plus w-8 h-8 flex items-center justify-center text-gray-600 hover:bg-gray-100 rounded-full focus:outline-none">+</button>
            </div>
          </div>
          <!-- Add to Cart Button -->
          <button data-product-id="<%= product._id %>" class="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-4 rounded-full transition duration-300 ease-in-out transform hover:scale-105 add-to-cart-btn <%= product.quantity === 0 ? 'opacity-50 cursor-not-allowed' : '' %>" <%= product.quantity === 0 ? 'disabled' : '' %>>
            <span class="add-to-cart-text flex items-center justify-center">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
              <%= product.quantity > 0 ? 'Add to Cart' : 'Out of Stock' %>
            </span>
          </button>
        </div>


        <!-- Like Button -->
        <button class="like-button absolute top-4 right-4 bg-white rounded-full p-2 shadow-md hover:bg-red-50 transition-colors duration-300" data-liked="false" data-product-id="<%= product._id %>">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
          </svg>
        </button>
      </div>

      <% }) %>
      </div>
    </section>


    <!-- Services -->
    <section class="service-section py-16 bg-transparent">
      <div class="container mx-auto px-4">
        <div class="heading-container flex items-center justify-center">
          <h2 class="text-4xl font-bold gradient-text mb-2">
            Services
          </h2>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div class="service-card bg-white bg-opacity-80 p-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-300 animate-slide-up">
            <div class="icon-wrapper mb-4 p-4 rounded-full bg-blue-100 inline-block relative overflow-hidden">
              <i class="fas fa-truck text-3xl text-blue-500 animate-truck"></i>
            </div>
            <h3 class="text-xl font-semibold mb-2">FREE AND FAST DELIVERY</h3>
            <p class="text-gray-600">Free delivery for all orders over $140</p>
          </div>
          <div class="service-card bg-white bg-opacity-80 p-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-300 animate-slide-up" style="animation-delay: 0.2s;">
            <div class="icon-wrapper mb-4 p-4 rounded-full bg-green-100 inline-block relative overflow-hidden">
              <i class="fas fa-headset text-3xl text-green-500 animate-pulse"></i>
            </div>
            <h3 class="text-xl font-semibold mb-2">24/7 CUSTOMER SERVICE</h3>
            <p class="text-gray-600">Friendly 24/7 customer support</p>
          </div>
          <div class="service-card bg-white bg-opacity-80 p-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-300 animate-slide-up" style="animation-delay: 0.4s;">
            <div class="icon-wrapper mb-4 p-4 rounded-full bg-purple-100 inline-block relative overflow-hidden">
              <i class="fas fa-shield-alt text-3xl text-purple-500 animate-bounce"></i>
            </div>
            <h3 class="text-xl font-semibold mb-2">MONEY BACK GUARANTEE</h3>
            <p class="text-gray-600">We return money within 30 days</p>
          </div>
        </div>
      </div>
    </section>
  </main>
  <%- include('../layouts/footer.ejs') %>

  <!-- JS -->
  <script src="/js/user-design-animation.js"></script>
  <script src="/js/product-cart.js"></script>
  <script src="/js/like-button.js"></script>
  <script src="/js/actions/likeFunction.js"></script>
  <script src="/js/actions/cartFunction.js"></script>
  <script src="/js/actions/quantity.js"></script>


  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-fade-in');
          }
        });
      }, {
        threshold: 0.1
      });

      document.querySelectorAll('.category-item').forEach(item => {
        observer.observe(item);
      });
    });
  </script>

</body>

</html>